# Challenges Dashboard - Component Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     KLARITI.SO FRONTEND                          │
│                    (Next.js 14 App Router)                       │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      RootLayout (layout.tsx)                     │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              AuthProvider (AuthContext.tsx)                │ │
│  │  Provides: { user, login, logout, isLoading }              │ │
│  │  Token Storage: localStorage                               │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                 ┌────────────┴────────────┐
                 ▼                         ▼
        ┌─────────────────┐      ┌─────────────────┐
        │   Navigation    │      │   Page Routes   │
        │  (Updated with  │      │                 │
        │  Challenges     │      │  /challenges    │
        │  link)          │      │  /playground    │
        └─────────────────┘      │  /auth          │
                                 └─────────────────┘


═══════════════════════════════════════════════════════════════════
                    /challenges PAGE STRUCTURE
═══════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│                    ChallengesPage Component                      │
│                     (/app/challenges/page.tsx)                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                   ProtectedRoute Wrapper                   │ │
│  │  (Checks auth, redirects to /auth if needed)              │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │               Page Header                            │ │ │
│  │  │  "Challenges" + Description                          │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │          ChallengeStats Component                    │ │ │
│  │  │  ┌──────┬──────┬──────┬──────┬──────┐              │ │ │
│  │  │  │Joined│Active│Done  │Create│Active│              │ │ │
│  │  │  │  🎯  │  🔥  │  ✅  │  ⭐  │ Crtd │              │ │ │
│  │  │  │   5  │   3  │   2  │   2  │  🚀  │              │ │ │
│  │  │  └──────┴──────┴──────┴──────┴──────┘              │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │  CreateChallengeForm (conditionally shown)          │ │ │
│  │  │  ┌────────────────────────────────────────────────┐ │ │ │
│  │  │  │  Name:        [30-Day No Social Media    ]     │ │ │ │
│  │  │  │  Description: [Stay off social media...  ]     │ │ │ │
│  │  │  │  Type:        [Time-Based] [Toggle]            │ │ │ │
│  │  │  │  Start Date:  [2025-11-16 00:00]               │ │ │ │
│  │  │  │  End Date:    [2025-12-16 23:59]               │ │ │ │
│  │  │  │  Strict Mode: [x] Enable                       │ │ │ │
│  │  │  │  Websites:    [https://facebook.com] [Add]     │ │ │ │
│  │  │  │               - Facebook.com         [Remove]  │ │ │ │
│  │  │  │               - Twitter.com          [Remove]  │ │ │ │
│  │  │  │  [Create Challenge] [Cancel]                   │ │ │ │
│  │  │  └────────────────────────────────────────────────┘ │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │           ChallengeList Component                    │ │ │
│  │  │  ┌────────────────────────────────────────────────┐ │ │ │
│  │  │  │  Tab Navigation                                │ │ │ │
│  │  │  │  [All Challenges] [My Challenges] [Created]   │ │ │ │
│  │  │  │                              [+ New Challenge] │ │ │ │
│  │  │  └────────────────────────────────────────────────┘ │ │ │
│  │  │                                                      │ │ │
│  │  │  ┌────────────────────────────────────────────────┐ │ │ │
│  │  │  │         ChallengeCard (Grid View)             │ │ │ │
│  │  │  │  ┌────────────────────────────────────────┐   │ │ │ │
│  │  │  │  │ 30-Day No Social Media      [Active] │   │ │ │ │
│  │  │  │  │ Stay off social media for 30 days    │   │ │ │ │
│  │  │  │  │ Type: Time-Based                     │   │ │ │ │
│  │  │  │  │ Duration: Nov 16 → Dec 16            │   │ │ │ │
│  │  │  │  │ [Strict Mode] [Creator]              │   │ │ │ │
│  │  │  │  │ Blocked: Facebook, Twitter           │   │ │ │ │
│  │  │  │  │          [Join Challenge]            │   │ │ │ │
│  │  │  │  └────────────────────────────────────────┘   │ │ │ │
│  │  │  │                                                │ │ │ │
│  │  │  │  ┌────────────────────────────────────────┐   │ │ │ │
│  │  │  │  │ Deep Work Mode          [Inactive]   │   │ │ │ │
│  │  │  │  │ Block distractions when focused      │   │ │ │ │
│  │  │  │  │ Type: Toggle                         │   │ │ │ │
│  │  │  │  │ [Creator]                            │   │ │ │ │
│  │  │  │  │ Blocked: YouTube, Reddit             │   │ │ │ │
│  │  │  │  │          [Activate]                  │   │ │ │ │
│  │  │  │  └────────────────────────────────────────┘   │ │ │ │
│  │  │  │                                                │ │ │ │
│  │  │  │  ... more challenge cards ...                 │ │ │ │
│  │  │  └────────────────────────────────────────────────┘ │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════
                       DATA FLOW ARCHITECTURE
═══════════════════════════════════════════════════════════════════

┌─────────────┐
│   Browser   │
│   User UI   │
└──────┬──────┘
       │
       │ User Action (Create, Join, etc.)
       ▼
┌─────────────────────┐
│  React Component    │
│  (ChallengeList,    │
│   CreateForm, etc.) │
└──────┬──────────────┘
       │
       │ Call service function
       ▼
┌─────────────────────────────────┐
│   Service Layer                 │
│   /src/services/challenges.ts   │
│                                 │
│  - createChallenge()            │
│  - getAllChallenges()           │
│  - joinChallenge()              │
│  - getMyChallenges()            │
│  - etc.                         │
└──────┬──────────────────────────┘
       │
       │ HTTP Request + JWT Token
       │ (from localStorage)
       ▼
┌─────────────────────────────────┐
│   FastAPI Backend               │
│   api-klariti (Port 8081)       │
│                                 │
│  Endpoints:                     │
│  POST   /challenges/            │
│  GET    /challenges/            │
│  GET    /challenges/{id}        │
│  PATCH  /challenges/{id}        │
│  POST   /challenges/{id}/join   │
│  etc.                           │
└──────┬──────────────────────────┘
       │
       │ Database Query
       ▼
┌─────────────────────────────────┐
│   SQLite Database               │
│   (auth.db)                     │
│                                 │
│  Tables:                        │
│  - users                        │
│  - challenges                   │
│  - time_based_details           │
│  - toggle_details               │
│  - distracting_websites         │
│  - user_challenge_participation │
└──────┬──────────────────────────┘
       │
       │ Query Result
       ▼
┌─────────────────────────────────┐
│   FastAPI Backend               │
│   (Process & Validate)          │
└──────┬──────────────────────────┘
       │
       │ JSON Response
       ▼
┌─────────────────────────────────┐
│   Service Layer                 │
│   (Parse & Type)                │
└──────┬──────────────────────────┘
       │
       │ Typed Data
       ▼
┌─────────────────────┐
│  React Component    │
│  (Update State)     │
└──────┬──────────────┘
       │
       │ Re-render
       ▼
┌─────────────┐
│   Browser   │
│   Updated   │
│   UI        │
└─────────────┘


═══════════════════════════════════════════════════════════════════
                      COMPONENT DEPENDENCY GRAPH
═══════════════════════════════════════════════════════════════════

AuthContext.tsx
    │
    ├─► Provides authentication to all components
    │
    ▼
ChallengesPage (page.tsx)
    │
    ├─► ChallengeStats
    │      │
    │      └─► calls: getMyChallenges()
    │               getMyCreatedChallenges()
    │
    ├─► CreateChallengeForm
    │      │
    │      └─► calls: createChallenge()
    │
    └─► ChallengeList
           │
           ├─► calls: getAllChallenges()
           │          getMyChallenges()
           │          getMyCreatedChallenges()
           │          joinChallenge()
           │          toggleChallengeStatus()
           │
           └─► ChallengeCard (multiple instances)
                  │
                  └─► displays: Challenge data
                      actions: Join, Toggle buttons


═══════════════════════════════════════════════════════════════════
                       TYPE DEFINITIONS FLOW
═══════════════════════════════════════════════════════════════════

OpenAPI Schema (Backend)
    │
    │ Defines API contract
    ▼
TypeScript Types (challenges.ts)
    │
    ├─► ChallengeType enum
    ├─► Challenge interface
    ├─► CreateChallengeData interface
    ├─► UpdateChallengeData interface
    ├─► DistractingWebsite interface
    ├─► TimeBasedDetails interface
    └─► ToggleDetails interface
            │
            │ Used by
            ▼
    All Components & Services
    (Type-safe throughout)


═══════════════════════════════════════════════════════════════════
                        AUTHENTICATION FLOW
═══════════════════════════════════════════════════════════════════

1. User logs in → JWT token stored in localStorage
                    │
2. Component calls service function
                    │
3. Service reads token from localStorage
                    │
4. Token added to Authorization header
                    │
5. Request sent to backend
                    │
6. Backend validates token
                    │
7. If valid: Process request
   If invalid: Return 401
                    │
8. Frontend handles response/error


═══════════════════════════════════════════════════════════════════
                         FILE DEPENDENCIES
═══════════════════════════════════════════════════════════════════

/src/services/challenges.ts
    │
    └─► /src/config/api.ts (API_BASE_URL)

/src/components/challenges/CreateChallengeForm.tsx
    │
    └─► /src/services/challenges.ts

/src/components/challenges/ChallengeCard.tsx
    │
    ├─► /src/services/challenges.ts (types)
    └─► /src/contexts/AuthContext.tsx (user data)

/src/components/challenges/ChallengeList.tsx
    │
    └─► /src/services/challenges.ts
    └─► /src/components/challenges/ChallengeCard.tsx

/src/components/challenges/ChallengeStats.tsx
    │
    └─► /src/services/challenges.ts

/src/app/challenges/page.tsx
    │
    ├─► /src/components/ProtectedRoute.tsx
    ├─► /src/contexts/AuthContext.tsx
    ├─► /src/components/challenges/ChallengeList.tsx
    ├─► /src/components/challenges/CreateChallengeForm.tsx
    └─► /src/components/challenges/ChallengeStats.tsx


═══════════════════════════════════════════════════════════════════
                     STATE MANAGEMENT FLOW
═══════════════════════════════════════════════════════════════════

Global State (AuthContext):
    - user: User | null
    - isLoading: boolean
    - login(), logout(), register()

Component Local State:

ChallengeList:
    - challenges: Challenge[]
    - isLoading: boolean
    - error: string | null
    - currentTab: TabType

CreateChallengeForm:
    - formData: CreateChallengeData
    - websites: DistractingWebsite[]
    - isSubmitting: boolean
    - error: string | null

ChallengeStats:
    - stats: ChallengeStats
    - isLoading: boolean


═══════════════════════════════════════════════════════════════════
                           RECAP
═══════════════════════════════════════════════════════════════════

✅ 11 Files Created/Modified
✅ Complete Type Safety (TypeScript)
✅ Authentication Integrated (JWT)
✅ Beautiful UI (Tailwind CSS)
✅ Error Handling (User-Friendly)
✅ Loading States (Smooth UX)
✅ Empty States (Helpful Messages)
✅ Responsive Design (Mobile-Ready)
✅ Modular Architecture (Reusable)
✅ Well Documented (3 MD files)
✅ Production Ready (No Errors)

```
